%table.table.table-striped 
  - @users.each do |user|
    %tr
      %td
        = link_to user.email_id, user_path(user)
      %td
        = link_to "Edit", edit_user_path(user)
      %td
        = link_to "Delete", user_path(user), method: :delete
    %tr
